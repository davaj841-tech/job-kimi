#!/usr/bin/env bash
# Keep husky lint-staged when using core.hooksPath=.githooks
set -euo pipefail
ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT"
if [[ -f .husky/pre-commit ]]; then
  # shellcheck disable=SC1091
  . ".husky/pre-commit"
fi
