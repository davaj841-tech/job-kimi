# سیاست امنیتی JobAzmoon

این سند نحوه پشتیبانی از نسخه‌ها، گزارش آسیب‌پذیری، و تدابیر امنیتی فعلی پلتفرم JobAzmoon را توضیح می‌دهد. با توجه به پرداخت آنلاین و نگهداری داده کاربران ایرانی، گزارش مسئولانه امنیتی برای ما اولویت دارد.

---

## نسخه‌های پشتیبانی‌شده (Supported Versions)

| نسخه / شاخه | پشتیبانی امنیتی |
|-------------|------------------|
| شاخه `main` | بله |
| آخرین تگ انتشار (مثلاً `v1.x.y`) | بله |
| شاخه‌ها و تگ‌های قدیمی‌تر | خیر |

وصله‌های امنیتی فقط روی `main` و آخرین تگ پایدار اعمال و منتشر می‌شوند. اگر روی نسخه قدیمی‌تر هستید، لطفاً به آخرین تگ ارتقا دهید.

---

## گزارش آسیب‌پذیری (Reporting a Vulnerability)

آسیب‌پذیری‌ها را **به‌صورت Issue عمومی** ثبت نکنید.

### کانال‌های مجاز

1. **ایمیل خصوصی:** [security@jobazmoon.ir](mailto:security@jobazmoon.ir)
2. **فرم / گزارش خصوصی GitHub:** در صورت فعال بودن، از مسیر  
   `Security` → `Report a vulnerability` در مخزن پروژه استفاده کنید.

### لطفاً در گزارش این موارد را بگنجانید

- نوع آسیب‌پذیری (مثلاً XSS، CSRF، IDOR، SSRF، نشت توکن، دستکاری پرداخت)
- شدت تقریبی و اثر روی کاربران یا داده مالی
- مراحل بازتولید یا PoC حداقلی
- نسخه / commit / محیط آزمایش‌شده
- پیشنهاد رفع (اختیاری)

### چه چیزی را ارسال نکنید

- رمز عبور واقعی کاربران
- توکن Sanctum یا کلیدهای `.env`
- داده‌های شخصی کامل کاربران بدون نیاز
- اسکن حجیم یا حمله DoS روی محیط production بدون هماهنگی

---

## زمان پاسخ (Response Time)

| مرحله | تعهد زمانی |
|--------|------------|
| تأیید دریافت گزارش | حداکثر **۴۸ ساعت** |
| رفع آسیب‌پذیری **critical** | هدف: حداکثر **۷ روز** پس از تأیید |
| آسیب‌پذیری‌های high / medium / low | بر اساس شدت، در نزدیک‌ترین انتشار پایدار |

پس از رفع و انتشار، در صورت تمایل گزارش‌دهنده، نام او در بخش [تالار افتخار](#تالار-افتخار-hall-of-fame) ذکر می‌شود. تا زمان انتشار وصله، لطفاً جزئیات را عمومی نکنید (responsible disclosure).

---

## تدابیر امنیتی فعلی (Security Measures)

| تدبیر | توضیح |
|--------|--------|
| **Sanctum tokens** | احراز هویت API با Laravel Sanctum؛ توکن‌های شخصی برای نشست‌های کاربر |
| **CSP headers** | هدر `Content-Security-Policy` و گزارش تخلف به مسیر CSP report |
| **Turnstile anti-bot** | Cloudflare Turnstile روی جریان‌های احراز هویت (در صورت پیکربندی کلیدها) |
| **Idempotent payment verify** | verify پرداخت با کلید ایدمپوتنسی برای جلوگیری از شارژ/اعتبار تکراری |
| **محافظت SQL Injection** | دسترسی به پایگاه‌داده عمدتاً از طریق Eloquent / Query Builder و binding پارامترها |
| **محافظت XSS** | خروجی پیش‌فرض Vue با escape؛ اجتناب از `v-html` ناامن روی داده کاربر |

سایر لایه‌های مرتبط:

- Secretها فقط در `.env` (بدون commit)
- Trust Proxies برای پروکسی‌های معتبر
- محدودیت نرخ (Rate limiting) روی OTP، پرداخت و API حساس
- محافظت SSRF در crawler با دامنه سفید
- کنترل دسترسی با Spatie Permission در پنل ادمین

---

## محدودیت‌های شناخته‌شده (Known Limitations)

- وابستگی‌های PHP باید به‌صورت دوره‌ای با دستور زیر بررسی شوند:

```bash
composer audit
```

- نتیجه بررسی روی نسخه قفل‌شده فعلی (`laravel/framework` **v11.55.1**، تاریخ بررسی: ۱۴۰۴ / مطابق محیط توسعه):

  | شدت | عنوان | مرجع |
  |-----|--------|------|
  | medium | Temporary Signed URL Path Confusion | [GHSA-crmm-hgp2-wgrp](https://github.com/advisories/GHSA-crmm-hgp2-wgrp) |
  | high | CRLF injection در قانون پیش‌فرض email | [GHSA-5vg9-5847-vvmq](https://github.com/advisories/GHSA-5vg9-5847-vvmq) |
  | — | Laravel CRLF injection (`CVE-2026-48019`) | [Advisory](https://github.com/laravel/framework/security/advisories/GHSA-5vg9-5847-vvmq) |

  بازه اعلام‌شده advisory شامل خط Laravel 11 است؛ رفع کامل ممکن است نیازمند ارتقا به وصله‌های Laravel 12.x یا دریافت backport رسمی باشد. وضعیت را با `composer audit` و release notes لاراول پیگیری کنید.

- `composer audit` همه ریسک‌های منطقی اپلیکیشن (مثلاً IDOR سفارشی) را پوشش نمی‌دهد؛ بازبینی کد و تست امنیتی همچنان لازم است.
- وابستگی‌های فرانت‌اند را جداگانه با ابزارهای npm (مثل `npm audit`) بررسی کنید.
- محیط production باید HTTPS، بکاپ رمزنگاری‌شده، و محدودسازی دسترسی به Horizon / Telescope / Filament داشته باشد.

---

## تالار افتخار (Hall of Fame)

از پژوهشگرانی که با اجازه خودشان گزارش مسئولانه داده‌اند قدردانی می‌کنیم:

| نام / شناسه | حوزه گزارش | تاریخ (تقریبی) |
|-------------|------------|----------------|
| — | هنوز موردی ثبت نشده | — |

برای افزوده‌شدن به این فهرست، در ایمیل گزارش صریحاً رضایت خود را برای ذکر نام اعلام کنید. در صورت تمایل می‌توانید با نام مستعار ذکر شوید.

---

## تماس

- امنیت: [security@jobazmoon.ir](mailto:security@jobazmoon.ir)
- مشارکت عمومی و باگ غیرحساس: [`CONTRIBUTING.md`](CONTRIBUTING.md) و GitHub Issues
